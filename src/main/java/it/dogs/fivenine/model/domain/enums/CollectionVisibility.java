package it.dogs.fivenine.model.domain.enums;

public enum CollectionVisibility {
    PRIVATE,    // Solo il proprietario può vedere
    PUBLIC,     // Tutti possono vedere
    FRIENDS     // Solo le connections possono vedere
}